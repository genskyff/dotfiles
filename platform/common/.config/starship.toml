"$schema" = "https://starship.rs/config-schema.json"

[aws]
disabled = true
{%- if os() == "linux" %}

[custom.wsl]
shell = ["sh"]
when = "test -f /etc/wsl-distribution.conf"
format = "[in \ue70f wsl]($style) "
style = "bold yellow"
{%- endif %}
